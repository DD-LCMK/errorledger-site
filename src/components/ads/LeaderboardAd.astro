---
/**
 * LeaderboardAd — Neutralized to prevent external ad redirects.
 */
interface Props {
  streamerMode?: boolean;
  class?: string;
}

const { streamerMode = false, class: className = '' } = Astro.props;
---
<!-- Ad disabled to prevent unwanted external redirects -->

