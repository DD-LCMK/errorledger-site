---
/**
 * DesktopWingAd — Desktop vertical gutter ad.
 * Disabled by default across all pages for a clean, distraction-free user experience.
 */
interface Props {
  side?: 'left' | 'right';
  streamerMode?: boolean;
  adKey?: string;
}

const {
  side = 'left',
  streamerMode = false,
  adKey = '491dd9dd2fa88cc7f03b2fcab1f83062',
} = Astro.props;
---

<!-- Ads disabled by default across all pages -->
