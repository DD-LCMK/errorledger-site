---
/**
 * DesktopWingAd — Neutralized to prevent external ad redirects.
 */
interface Props {
  side?: 'left' | 'right';
  streamerMode?: boolean;
  adKey?: string;
}

const {
  side = 'left',
  streamerMode = false,
  adKey = '',
} = Astro.props;
---
<!-- Ad disabled to prevent unwanted external redirects -->

