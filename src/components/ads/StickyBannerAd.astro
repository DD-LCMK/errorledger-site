---
/**
 * StickyBannerAd — Neutralized to prevent external ad redirects.
 */
interface Props {
  streamerMode?: boolean;
}
const { streamerMode = false } = Astro.props;
---
<!-- Ad disabled to prevent unwanted external redirects -->

