---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');
const enPosts = allPosts.filter(p => !p.id.startsWith('ko/') && (p.data.lang || 'en') === 'en');

const posts = enPosts.sort((a, b) => {
	const dateA = a.data.pubDate ? new Date(a.data.pubDate).valueOf() : 0;
	const dateB = b.data.pubDate ? new Date(b.data.pubDate).valueOf() : 0;
	return dateB - dateA;
});

const categoryCounts: Record<string, number> = {
	all: posts.length,
	corporate: posts.filter(p => p.data.category === 'corporate').length,
	money: posts.filter(p => p.data.category === 'money').length,
	work: posts.filter(p => p.data.category === 'work').length,
	ai: posts.filter(p => p.data.category === 'ai').length,
	internet: posts.filter(p => p.data.category === 'internet').length,
	relationships: posts.filter(p => p.data.category === 'relationships').length,
	human: posts.filter(p => p.data.category === 'human').length,
};

const categories = [
	{ id: "all", label: "⚡ All Incidents", icon: "⚡" },
	{ id: "corporate", label: "🏢 Corporate & Systems", icon: "🏢" },
	{ id: "money", label: "💰 Money & Finance", icon: "💰" },
	{ id: "work", label: "🧑‍💻 Work & Career", icon: "🧑‍💻" },
	{ id: "ai", label: "🤖 AI & Automation", icon: "🤖" },
	{ id: "internet", label: "🌐 Internet & Social", icon: "🌐" },
	{ id: "relationships", label: "❤️ Relationships", icon: "❤️" },
	{ id: "human", label: "🧠 Human Psychology", icon: "🧠" },
];
---

<html lang="en" class="dark">
	<head>
		<BaseHead title={`The Failure Ledger Archive | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
		<style>
			main { width: 1160px; max-width: calc(100% - 2rem); margin: 0 auto; padding: 3em 1rem; }

			.archive-header {
				margin-bottom: 2.5rem;
				border-bottom: 1px solid #262626;
				padding-bottom: 1.5rem;
			}
			.archive-header h1 {
				font-size: 2.5rem;
				font-weight: 800;
				color: #f5f5f5;
				margin: 0 0 0.5rem 0;
				letter-spacing: -0.03em;
			}
			.archive-header p {
				color: #a3a3a3;
				font-size: 1.1rem;
				margin: 0;
				line-height: 1.6;
			}

			/* 2-Column Responsive Archive Layout */
			.archive-layout-grid {
				display: grid;
				grid-template-columns: 1fr;
				gap: 2rem;
				align-items: start;
			}
			@media (min-width: 1024px) {
				.archive-layout-grid {
					grid-template-columns: 280px 1fr;
				}
			}

			/* Left Sticky Category Rail */
			.category-rail {
				background: #171717;
				border: 1px solid #262626;
				border-radius: 16px;
				padding: 1.25rem;
				position: sticky;
				top: 5.5rem;
				max-height: calc(100vh - 7rem);
				overflow-y: auto;
			}
			@media (max-width: 1023px) {
				.category-rail {
					position: relative;
					top: 0;
					margin-bottom: 1.5rem;
					padding: 1rem;
				}
			}

			.rail-header {
				font-family: ui-monospace, monospace;
				font-size: 0.75rem;
				font-weight: 700;
				text-transform: uppercase;
				color: #a3a3a3;
				letter-spacing: 0.05em;
				margin-bottom: 0.85rem;
				padding-bottom: 0.5rem;
				border-bottom: 1px solid #262626;
				display: flex;
				justify-content: space-between;
				align-items: center;
			}

			.category-nav-list {
				display: flex;
				flex-direction: column;
				gap: 0.4rem;
			}
			@media (max-width: 1023px) {
				.category-nav-list {
					flex-direction: row;
					flex-wrap: wrap;
					gap: 0.5rem;
				}
			}

			.category-nav-btn {
				display: flex;
				align-items: center;
				justify-content: space-between;
				width: 100%;
				background: transparent;
				border: 1px solid transparent;
				color: #d4d4d4;
				padding: 0.6rem 0.85rem;
				border-radius: 8px;
				font-size: 0.88rem;
				font-weight: 600;
				cursor: pointer;
				text-align: left;
				transition: all 0.15s ease;
				box-sizing: border-box;
				font-family: inherit;
			}
			@media (max-width: 1023px) {
				.category-nav-btn {
					width: auto;
					padding: 0.45rem 0.8rem;
					border: 1px solid #262626;
					background: #0a0a0a;
					border-radius: 9999px;
					font-size: 0.8rem;
				}
			}
			.category-nav-btn:hover {
				background: #262626;
				color: #ffffff;
			}
			.category-nav-btn.active {
				background: rgba(244, 63, 94, 0.12);
				border-color: rgba(244, 63, 94, 0.3);
				color: #fb7185;
			}

			.cat-count-badge {
				font-family: ui-monospace, monospace;
				font-size: 0.72rem;
				background: #0a0a0a;
				color: #737373;
				padding: 0.1rem 0.45rem;
				border-radius: 9999px;
				border: 1px solid #262626;
			}
			.category-nav-btn.active .cat-count-badge {
				background: #f43f5e;
				color: #ffffff;
				border-color: #f43f5e;
			}

			.rail-dossiers {
				margin-top: 1.5rem;
				padding-top: 1.25rem;
				border-top: 1px solid #262626;
			}
			@media (max-width: 1023px) {
				.rail-dossiers {
					display: none;
				}
			}
			.dossier-link {
				display: block;
				font-size: 0.82rem;
				color: #a3a3a3;
				text-decoration: none;
				padding: 0.35rem 0;
				transition: color 0.15s ease;
			}
			.dossier-link:hover {
				color: #f59e0b;
			}

			/* Right Incident Stream & Search */
			.stream-controls {
				display: flex;
				align-items: center;
				justify-content: space-between;
				margin-bottom: 1.5rem;
				gap: 1rem;
				flex-wrap: wrap;
			}
			.stream-heading {
				font-size: 1.1rem;
				font-weight: 800;
				color: #f5f5f5;
				text-transform: uppercase;
				letter-spacing: 0.025em;
			}

			.live-filter-input {
				background: #171717;
				border: 1px solid #262626;
				color: #f5f5f5;
				font-size: 0.85rem;
				padding: 0.5rem 0.85rem;
				border-radius: 8px;
				outline: none;
				width: 100%;
				max-width: 320px;
				font-family: inherit;
				transition: border-color 0.15s ease;
			}
			.live-filter-input:focus {
				border-color: #f43f5e;
			}

			.grid-archive {
				display: grid;
				grid-template-columns: 1fr;
				gap: 1.25rem;
			}

			.archive-card {
				background: #171717;
				border: 1px solid #262626;
				border-radius: 12px;
				padding: 1.5rem;
				transition: transform 0.2s ease, border-color 0.2s ease;
				text-decoration: none;
				display: block;
			}
			.archive-card:hover {
				transform: translateY(-2px);
				border-color: #f43f5e;
			}

			.card-meta {
				display: flex;
				align-items: center;
				gap: 0.6rem;
				margin-bottom: 0.6rem;
				font-size: 0.75rem;
				color: #737373;
				font-family: ui-monospace, monospace;
				flex-wrap: wrap;
			}
			.card-cat-tag {
				padding: 0.15rem 0.5rem;
				border-radius: 4px;
				background: #0a0a0a;
				border: 1px solid #262626;
				color: #d4d4d4;
				font-weight: 700;
				text-transform: uppercase;
			}
			.card-archive-title {
				font-size: 1.25rem;
				font-weight: 700;
				color: #f5f5f5;
				margin: 0 0 0.5rem 0;
				line-height: 1.35;
				transition: color 0.15s ease;
			}
			.archive-card:hover .card-archive-title {
				color: #fb7185;
			}
			.card-archive-desc {
				font-size: 0.88rem;
				color: #a3a3a3;
				line-height: 1.5;
				margin: 0;
			}

			.hidden { display: none !important; }

			/* Light mode styles */
			:global(html:not(.dark)) .archive-header {
				border-bottom-color: #e2e8f0;
			}
			:global(html:not(.dark)) .archive-header h1 {
				color: #0f172a;
			}
			:global(html:not(.dark)) .archive-header p {
				color: #64748b;
			}
			:global(html:not(.dark)) .category-rail,
			:global(html:not(.dark)) .archive-card {
				background: #f8fafc;
				border-color: #e2e8f0;
			}
			:global(html:not(.dark)) .rail-header {
				border-bottom-color: #e2e8f0;
				color: #64748b;
			}
			:global(html:not(.dark)) .category-nav-btn {
				color: #334155;
			}
			:global(html:not(.dark)) .category-nav-btn:hover {
				background: #e2e8f0;
				color: #0f172a;
			}
			:global(html:not(.dark)) .category-nav-btn.active {
				background: #fee2e2;
				border-color: #fca5a5;
				color: #e11d48;
			}
			:global(html:not(.dark)) .cat-count-badge {
				background: #ffffff;
				border-color: #cbd5e1;
				color: #475569;
			}
			:global(html:not(.dark)) .stream-heading {
				color: #0f172a;
			}
			:global(html:not(.dark)) .live-filter-input {
				background: #f8fafc;
				border-color: #cbd5e1;
				color: #0f172a;
			}
			:global(html:not(.dark)) .archive-card:hover {
				background: #ffffff;
			}
			:global(html:not(.dark)) .card-archive-title {
				color: #0f172a;
			}
			:global(html:not(.dark)) .card-archive-desc {
				color: #475569;
			}
			:global(html:not(.dark)) .card-cat-tag {
				background: #ffffff;
				border-color: #cbd5e1;
				color: #334155;
			}
		</style>
	</head>
	<body class="bg-neutral-950 text-neutral-200 antialiased min-h-screen">
		<Header />
		<main>
			<div class="archive-header">
				<h1>The Failure Ledger Archive</h1>
				<p>Investigative records of human error, corporate negligence, and systemic breakdowns. Select a category on the left to filter the ledger.</p>
			</div>

			<div class="archive-layout-grid">
				<!-- Left Sticky Category Rail -->
				<aside class="category-rail">
					<div class="rail-header">
						<span>Categories</span>
						<span>{posts.length} Files</span>
					</div>
					<div class="category-nav-list" id="category-filter-list">
						{categories.map((cat) => (
							<button 
								class={`category-nav-btn ${cat.id === 'all' ? 'active' : ''}`}
								data-category-filter={cat.id}
							>
								<span>{cat.label}</span>
								<span class="cat-count-badge">{categoryCounts[cat.id] || 0}</span>
							</button>
						))}
					</div>

					<div class="rail-dossiers">
						<div class="rail-header" style="border: none; margin-bottom: 0.5rem; padding-bottom: 0;">
							<span>Curated Dossiers</span>
						</div>
						<a href="/blog/deleted-production-database-blamed-intern" class="dossier-link">
							📂 The Scapegoat Collection
						</a>
						<a href="/blog/knight-capital-trading-glitch-45-minutes" class="dossier-link">
							⚡ 45-Minute Fast Crashes
						</a>
						<a href="/blog/the-friday-update-that-broke-the-world" class="dossier-link">
							🌋 Cloud Monoculture Failures
						</a>
					</div>
				</aside>

				<!-- Right Incident Stream -->
				<section class="archive-stream">
					<div class="stream-controls">
						<div class="stream-heading" id="current-filter-title">
							All Incidents
						</div>
						<input 
							type="text" 
							id="archive-search" 
							class="live-filter-input" 
							placeholder="Search by title or keywords..." 
							autocomplete="off"
						/>
					</div>

					<div class="grid-archive" id="archive-grid">
						{posts.map((post) => (
							<a 
								href={`/blog/${post.data.shortenedSlug || post.slug}`} 
								class="archive-card" 
								data-category={post.data.category}
								data-title={post.data.title.toLowerCase()}
							>
								<div class="card-meta">
									<span class="card-cat-tag">
										{post.data.category}
									</span>
									<span>•</span>
									<FormattedDate date={post.data.pubDate || new Date()} />
									<span>•</span>
									<span>⏱️ {post.data.read_time_minutes || 6}m read</span>
									<span>•</span>
									<span style="color: #fb7185;">{post.data.provenance_label || "Documented Incident"}</span>
								</div>
								<h3 class="card-archive-title">
									{post.data.title}
								</h3>
								<p class="card-archive-desc">
									{post.data.description}
								</p>
							</a>
						))}
					</div>

					<div id="no-results" class="hidden" style="text-align: center; padding: 4rem 0; color: #737373; font-family: ui-monospace, monospace; font-size: 0.9rem;">
						No case files found matching this filter.
					</div>
				</section>
			</div>
		</main>
		<Footer />

		<script is:inline>
			(function() {
				const catButtons = document.querySelectorAll('[data-category-filter]');
				const searchInput = document.getElementById('archive-search');
				const cards = document.querySelectorAll('.archive-card');
				const noResults = document.getElementById('no-results');
				const streamTitle = document.getElementById('current-filter-title');

				let activeCategory = 'all';
				let searchQuery = '';

				function applyFilters() {
					let visibleCount = 0;
					cards.forEach(card => {
						const cat = card.getAttribute('data-category');
						const title = card.getAttribute('data-title') || '';

						const matchesCategory = (activeCategory === 'all') || (cat === activeCategory);
						const matchesSearch = !searchQuery || title.includes(searchQuery.toLowerCase());

						if (matchesCategory && matchesSearch) {
							card.style.display = 'block';
							visibleCount++;
						} else {
							card.style.display = 'none';
						}
					});

					if (noResults) {
						if (visibleCount === 0) noResults.classList.remove('hidden');
						else noResults.classList.add('hidden');
					}
				}

				catButtons.forEach(btn => {
					btn.addEventListener('click', function() {
						catButtons.forEach(b => b.classList.remove('active'));
						this.classList.add('active');
						activeCategory = this.getAttribute('data-category-filter') || 'all';
						
						if (streamTitle) {
							const label = this.querySelector('span:first-child')?.innerText || 'All Incidents';
							streamTitle.innerText = label;
						}
						applyFilters();
					});
				});

				searchInput?.addEventListener('input', function() {
					searchQuery = this.value.trim();
					applyFilters();
				});
			})();
		</script>
	</body>
</html>