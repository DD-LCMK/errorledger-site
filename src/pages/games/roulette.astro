---
import GameLayout from '../../layouts/GameLayout.astro';
import { getEntry } from 'astro:content';

const gameEntry = await getEntry('games', 'roulette');
const data = gameEntry?.data || {
  title: '스트리머 돌림판 룰렛',
  tagline: '방송 & 추첨 전용 커스텀 행운의 룰렛',
  description: '야식 메뉴 결정, 벌칙 추첨, 구독자 이벤트까지! 부드러운 물리 회전과 1초 프리셋을 지원하는 무료 방송용 웹 룰렛입니다.',
  icon: '🎡',
  category: 'stream',
  badge: '🔥 방송 필수',
  players: '1인 ~ 시청자 다수',
  playTime: '즉시 (3~10초)',
  controls: [
    '스페이스바(Space) 또는 [룰렛 돌리기] 버튼 클릭',
    '오른쪽 목록에서 나만의 항목 및 가중치 직접 입력',
    '상단 원클릭 프리셋으로 야식, 벌칙, 선물 추첨 즉시 세팅',
    '집중 모드를 활용해 깔끔한 전체화면 연출 가능'
  ],
  rules: [
    '각 항목은 돌림판 위의 화려한 부채꼴 칸으로 자동 배정됩니다.',
    '물리 엔진 기반의 무작위 회전 토크와 감속 마찰을 거쳐 회전합니다.',
    '상단 바늘이 최종 멈춘 칸이 당첨자로 선정되며 꽃가루 축하 효과가 울립니다!'
  ],
  faqItems: [
    {
      q: '전체화면이나 집중 모드는 어떻게 사용하나요?',
      a: '하단의 [📺 집중 모드] 버튼이나 상단 [⛶ 전체화면]을 누르면 불필요한 브라우저 여백 없이 돌림판을 화면 가득 시원하게 띄워 진행할 수 있습니다.'
    },
    {
      q: '한 번 뽑힌 항목을 다음 라운드에서 자동 제외할 수 있나요?',
      a: '네! 제어판에서 [당첨 항목 제외] 옵션을 켜두시면 여러 번 연속 추첨할 때 이미 뽑힌 항목이 자동으로 목록에서 제외됩니다.'
    }
  ]
};
---

<GameLayout
  title={data.title}
  description={data.description}
  tagline={data.tagline}
  icon={data.icon}
  category={data.category}
  badge={data.badge}
  players={data.players}
  playTime={data.playTime}
  controls={data.controls}
  rules={data.rules}
  faqItems={data.faqItems}
  layoutMode="full"
  hideStageHeader={true}
  hideFooter={false}
>
  <div class="roulette-app-root" id="roulette-root">
    <!-- Carnival Neon & Golden Sparkles Theme Canvas -->
    <canvas id="carnival-canvas" class="carnival-canvas"></canvas>

    <!-- Left / Center: The Wheel Stage -->
    <div class="wheel-stage-container">
      <div class="wheel-title-bar" id="display-title">오늘의 방송 룰렛</div>

      <div class="wheel-stage-layout">
        <!-- Probability & Items Panel (Utilizing Stage Left Space) -->
        <div class="roulette-prob-panel" id="roulette-prob-panel">
          <div class="prob-panel-header">
            <div class="prob-title-row">
              <span class="prob-icon">📊</span>
              <span class="prob-title-text" id="prob-title-text">항목 및 당첨 확률</span>
            </div>
            <span class="prob-total-badge" id="prob-total-badge">총 0개 항목</span>
          </div>
          <div class="prob-items-list" id="prob-items-list">
            <!-- Dynamically populated in JS -->
          </div>
        </div>

        <div class="wheel-canvas-wrapper" id="wheel-wrapper">
          <!-- Physical Pointer Needle with Live Arrow Ticker -->
          <div class="roulette-pointer" id="wheel-pointer">
            <div class="pointer-live-ticker" id="pointer-ticker">
              <span class="ticker-dot" id="ticker-dot"></span>
              <span class="ticker-text" id="ticker-text">준비 완료 (Space)</span>
            </div>
            <div class="pointer-arrow"></div>
          </div>

          <!-- HTML5 High-DPI Canvas -->
          <canvas id="roulette-canvas" width="600" height="600"></canvas>

          <!-- Center Glowing Hub & Spin Button -->
          <button id="center-spin-btn" class="center-hub-btn" title="클릭하거나 스페이스바를 눌러 룰렛 회전">
            <span class="hub-label">돌리기</span>
          </button>
        </div>
      </div>

      <!-- Quick Action Controls -->
      <div class="wheel-action-bar">
        <button id="main-spin-btn" class="roulette-btn spin-btn-main">
          <span>🎯 룰렛 돌리기 (Space)</span>
        </button>
        <button id="shuffle-btn" class="roulette-btn btn-secondary">
          <span>🔀 항목 섞기</span>
        </button>
        <button id="stream-mode-btn" class="roulette-btn btn-stream" title="깔끔한 집중 모드 전환">
          <span>📺 집중 모드</span>
        </button>
      </div>
    </div>

    <!-- Right: Streamer Control Desk -->
    <div class="streamer-sidebar" id="streamer-sidebar">
      <div class="sidebar-header">
        <span class="panel-icon">⚙️</span>
        <h3>룰렛 설정 데스크</h3>
      </div>

      <!-- Title Input -->
      <div class="config-group">
        <label for="input-title">룰렛 / 추첨 제목</label>
        <input type="text" id="input-title" value="오늘의 방송 룰렛" placeholder="룰렛 제목을 입력하세요" />
      </div>

      <!-- Presets Row -->
      <div class="config-group">
        <label>⚡ 방송 추천 프리셋</label>
        <div class="presets-grid">
          <button class="preset-pill" data-preset="food">🍗 야식 메뉴</button>
          <button class="preset-pill" data-preset="penalty">⚡ 벌칙 룰렛</button>
          <button class="preset-pill" data-preset="event">🎁 치킨/이벤트</button>
          <button class="preset-pill" data-preset="game">🎮 게임 뽑기</button>
        </div>
      </div>

      <!-- Items Textarea -->
      <div class="config-group">
        <div class="label-with-meta">
          <label for="input-items">항목 목록</label>
          <span class="items-count-badge" id="items-count-badge">8개 항목</span>
        </div>
        <p class="input-hint-text">💡 팁: <code>피자*2</code>, <code>치킨*3</code> 처럼 입력하면 여러 칸이 자동 생성됩니다!</p>
        <textarea id="input-items" rows="5" placeholder="항목을 줄바꿈으로 입력하세요 (예: 피자*2, 치킨*3, 꽝*5)">치킨
피자*2
햄버거
족발
엽기떡볶이
마라탕
삼겹살
라면</textarea>
      </div>

      <!-- Options Toggles -->
      <div class="config-group toggles-row">
        <label class="checkbox-label">
          <input type="checkbox" id="chk-eliminate" />
          <span>당첨된 항목 자동 제외</span>
        </label>
        <label class="checkbox-label">
          <input type="checkbox" id="chk-sound" checked />
          <span>효과음 (틱 & 팡파레)</span>
        </label>
      </div>

      <!-- Recent Winners History -->
      <div class="config-group history-group">
        <div class="label-with-meta">
          <label>🏆 당첨 히스토리</label>
          <button id="clear-history-btn" class="mini-clear-btn">초기화</button>
        </div>
        <div class="winners-history-list" id="winners-history">
          <span class="empty-history-text">아직 당첨 기록이 없습니다. 룰렛을 돌려보세요!</span>
        </div>
      </div>
    </div>

    <!-- Celebration Modal / Banner Overlay -->
    <div class="winner-celebration-overlay hidden" id="winner-modal">
      <div class="celebration-card">
        <div class="trophy-bounce">👑</div>
        <div class="congrats-tag">🎉 당첨을 축하합니다!</div>
        <div class="winner-item-name" id="winner-name-display">치킨</div>
        <button id="modal-confirm-btn" class="winner-confirm-btn">확인 (닫기)</button>
      </div>
    </div>

    <!-- Particle Confetti Canvas Layer -->
    <canvas id="confetti-canvas" class="confetti-layer"></canvas>
  </div>

  <div slot="guide">
    <h2>방송용 돌림판 룰렛 활용 가이드</h2>
    <p>
      <strong>스트리머 룰렛</strong>은 치지직(CHZZK), SOOP(아프리카TV), 유튜브 라이브 등 다양한 방송 환경에서 시청자와 함께 실시간으로 소통할 수 있도록 제작된 무료 웹 도구입니다.
    </p>

    <h3>1. 1초 원클릭 방송 프리셋</h3>
    <p>
      상단의 야식 메뉴, 벌칙 룰렛, 치킨 이벤트 프리셋 버튼을 클릭하면 방송에서 가장 많이 쓰이는 항목들이 돌림판에 즉시 자동 입력됩니다.
    </p>

    <h3>2. 가중치 설정 팁</h3>
    <p>
      특정 항목의 당첨 확률을 높이거나 여러 칸으로 만들고 싶다면 <code>피자*2</code>, <code>치킨*3</code>과 같이 별표(*)와 숫자를 붙여 입력해 보세요. 해당 칸이 자동으로 분할되어 생성됩니다.
    </p>
  </div>
</GameLayout>

<style>
  /* Floating Carnival Golden Sparkles Canvas */
  .carnival-canvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    pointer-events: none;
    z-index: 0;
  }

  .roulette-app-root {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(560px, 1fr) 340px;
    gap: 1.25rem;
    background: transparent;
    padding: 0;
    max-width: 100%;
    width: 100%;
    margin: 0;
    user-select: none;
    align-items: start;
  }

  @media (max-width: 1000px) {
    .roulette-app-root {
      grid-template-columns: 1fr;
    }
  }

  /* Wheel Stage */
  .wheel-stage-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    background: #0f172a;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 14px;
    padding: 0.85rem 1rem;
    position: relative;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
  }

  .wheel-title-bar {
    font-size: 1.3rem;
    font-weight: 900;
    color: #f8fafc;
    letter-spacing: -0.02em;
    margin-bottom: 0.5rem;
    text-shadow: 0 0 15px rgba(168, 85, 247, 0.4);
    text-align: center;
  }

  /* Wheel Stage Layout: Prob Panel + Canvas */
  .wheel-stage-layout {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    width: 100%;
    margin: 0.5rem 0;
  }

  /* Left Side: Probability & Items Panel */
  .roulette-prob-panel {
    width: 250px;
    height: 480px;
    background: rgba(10, 15, 29, 0.85);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 14px;
    padding: 0.85rem;
    display: flex;
    flex-direction: column;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
    flex-shrink: 0;
  }

  .prob-panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 0.6rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    margin-bottom: 0.6rem;
  }

  .prob-title-row {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .prob-icon {
    font-size: 1rem;
  }

  .prob-title-text {
    font-size: 0.88rem;
    font-weight: 800;
    color: #f8fafc;
    letter-spacing: -0.01em;
  }

  .prob-total-badge {
    font-size: 0.72rem;
    font-weight: 800;
    color: #38bdf8;
    background: rgba(56, 189, 248, 0.12);
    border: 1px solid rgba(56, 189, 248, 0.25);
    padding: 0.15rem 0.45rem;
    border-radius: 6px;
  }

  .prob-items-list {
    display: flex;
    flex-direction: column;
    gap: 0.32rem;
    overflow-y: auto;
    flex: 1;
    padding-right: 0.2rem;
  }

  .prob-items-list::-webkit-scrollbar {
    width: 4px;
  }
  .prob-items-list::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 4px;
  }

  .roulette-prob-panel :global(.prob-item-row) {
    position: relative;
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    background: rgba(255, 255, 255, 0.035);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 7px;
    height: 32px;
    min-height: 32px;
    padding: 0 0.55rem;
    gap: 0.35rem;
    transition: all 0.12s ease;
    overflow: hidden;
  }

  .roulette-prob-panel :global(.prob-item-row:hover) {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.15);
  }

  .roulette-prob-panel :global(.prob-item-row.is-active-slice) {
    background: rgba(255, 255, 255, 0.14);
    border-color: var(--item-color, #38bdf8);
    box-shadow: 0 0 14px var(--item-color, rgba(56, 189, 248, 0.45));
    transform: scale(1.02) translateX(2px);
  }

  .roulette-prob-panel :global(.prob-color-dot) {
    display: inline-block !important;
    width: 8px !important;
    height: 8px !important;
    border-radius: 50%;
    flex-shrink: 0;
    box-shadow: 0 0 6px var(--item-color, currentColor);
  }

  .roulette-prob-panel :global(.prob-item-name) {
    font-size: 0.78rem;
    font-weight: 700;
    color: #f1f5f9;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
  }

  .roulette-prob-panel :global(.prob-item-count) {
    font-size: 0.64rem;
    font-weight: 700;
    color: #94a3b8;
    background: rgba(255, 255, 255, 0.08);
    padding: 0.05rem 0.28rem;
    border-radius: 4px;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .roulette-prob-panel :global(.prob-inline-bar-track) {
    width: 38px;
    height: 4px;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 2px;
    overflow: hidden;
    flex-shrink: 0;
  }

  .roulette-prob-panel :global(.prob-inline-bar-fill) {
    height: 100%;
    border-radius: 2px;
    transition: width 0.3s ease;
  }

  .roulette-prob-panel :global(.prob-item-percent) {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.76rem;
    font-weight: 800;
    color: #38bdf8;
    background: rgba(56, 189, 248, 0.1);
    border: 1px solid rgba(56, 189, 248, 0.22);
    border-radius: 5px;
    padding: 0.06rem 0.35rem;
    flex-shrink: 0;
    min-width: 44px;
    text-align: right;
    letter-spacing: -0.02em;
  }

  .roulette-prob-panel :global(.prob-empty-text) {
    font-size: 0.78rem;
    color: #64748b;
    text-align: center;
    padding: 1rem 0;
    font-style: italic;
  }

  .wheel-canvas-wrapper {
    position: relative;
    width: 500px;
    height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  #roulette-canvas {
    width: 490px;
    height: 490px;
    border-radius: 50%;
    box-shadow: 0 0 35px rgba(0, 0, 0, 0.7), 0 0 25px rgba(168, 85, 247, 0.3);
  }

  /* Top Needle Pointer with Live Arrow Ticker */
  .roulette-pointer {
    position: absolute;
    top: -18px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 25;
    pointer-events: none;
    transition: transform 0.08s ease-out;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .pointer-live-ticker {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 120px;
    gap: 0.35rem;
    padding: 0.25rem 0.75rem;
    background: rgba(15, 23, 42, 0.95);
    border: 1.5px solid var(--ticker-color, #38bdf8);
    border-radius: 9999px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.7), 0 0 12px var(--ticker-color, rgba(56, 189, 248, 0.5));
    font-size: 0.78rem;
    font-weight: 800;
    color: #ffffff;
    white-space: nowrap;
    margin-bottom: 2px;
    transition: all 0.08s ease;
    backdrop-filter: blur(4px);
  }

  .ticker-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--ticker-color, #38bdf8);
    box-shadow: 0 0 6px var(--ticker-color, #38bdf8);
  }

  .ticker-text {
    max-width: 140px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .pointer-live-ticker.ticker-pulse {
    transform: scale(1.08);
  }

  .pointer-live-ticker.is-winner {
    background: linear-gradient(135deg, #facc15 0%, #f97316 100%);
    border-color: #fef08a;
    color: #020617;
    box-shadow: 0 0 20px rgba(250, 204, 21, 0.9);
    transform: scale(1.12);
  }

  .pointer-live-ticker.is-winner .ticker-dot {
    background: #020617;
    box-shadow: 0 0 6px #020617;
  }

  .pointer-arrow {
    width: 0;
    height: 0;
    border-left: 16px solid transparent;
    border-right: 16px solid transparent;
    border-top: 32px solid #ef4444;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.6)) drop-shadow(0 0 10px #ef4444);
  }

  .pointer-tick {
    transform: translateX(-50%) rotate(-12deg);
  }

  /* Center Hub Button */
  .center-hub-btn {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 72px;
    height: 72px;
    border-radius: 50%;
    background: radial-gradient(circle, #f8fafc 0%, #0f172a 100%);
    border: 4px solid #ffffff;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.8), 0 0 15px rgba(56, 189, 248, 0.6);
    color: #020617;
    cursor: pointer;
    z-index: 20;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.15s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.15s ease;
  }

  .center-hub-btn:hover {
    transform: translate(-50%, -50%) scale(1.1);
    box-shadow: 0 0 25px rgba(56, 189, 248, 0.9);
  }

  .hub-label {
    font-size: 0.88rem;
    font-weight: 900;
    letter-spacing: 0.05em;
  }

  /* Action Bar */
  .wheel-action-bar {
    display: flex;
    gap: 0.75rem;
    margin-top: 0.85rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .roulette-btn {
    padding: 0.75rem 1.5rem;
    border-radius: 10px;
    font-size: 0.95rem;
    font-weight: 800;
    cursor: pointer;
    border: none;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  .spin-btn-main {
    background: linear-gradient(135deg, #a855f7 0%, #38bdf8 100%);
    color: #ffffff;
    box-shadow: 0 0 20px rgba(168, 85, 247, 0.4);
  }

  .spin-btn-main:hover {
    transform: scale(1.04);
    box-shadow: 0 0 25px rgba(168, 85, 247, 0.6);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.08);
    color: #e2e8f0;
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.15);
    color: #ffffff;
  }

  .btn-stream {
    background: rgba(56, 189, 248, 0.15);
    color: #38bdf8;
    border: 1px solid rgba(56, 189, 248, 0.3);
  }

  .btn-stream:hover {
    background: rgba(56, 189, 248, 0.25);
  }

  /* Streamer Sidebar */
  .streamer-sidebar {
    background: #0f172a;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 14px;
    padding: 1rem 1.15rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
  }

  .sidebar-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding-bottom: 0.6rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .sidebar-header h3 {
    font-size: 1.05rem;
    font-weight: 800;
    color: #f8fafc;
    margin: 0;
  }

  .config-group {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .config-group label {
    font-size: 0.75rem;
    font-weight: 800;
    color: #94a3b8;
    letter-spacing: 0.05em;
  }

  .label-with-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .items-count-badge {
    font-size: 0.7rem;
    font-weight: 800;
    color: #38bdf8;
    background: rgba(56, 189, 248, 0.12);
    padding: 0.15rem 0.45rem;
    border-radius: 4px;
  }

  .input-hint-text {
    font-size: 0.75rem;
    color: #a855f7;
    margin: 0;
    line-height: 1.4;
    font-weight: 600;
  }

  .input-hint-text code {
    background: rgba(168, 85, 247, 0.15);
    color: #f0abfc;
    padding: 0.1rem 0.3rem;
    border-radius: 4px;
  }

  #input-title, #input-items {
    background: #090d16;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 8px;
    color: #f8fafc;
    padding: 0.6rem 0.75rem;
    font-size: 0.88rem;
    outline: none;
    font-family: inherit;
    transition: border-color 0.15s ease;
  }

  #input-title:focus, #input-items:focus {
    border-color: #38bdf8;
  }

  #input-items {
    resize: vertical;
    line-height: 1.5;
  }

  /* Presets Grid */
  .presets-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.4rem;
  }

  .preset-pill {
    background: rgba(168, 85, 247, 0.12);
    border: 1px solid rgba(168, 85, 247, 0.25);
    color: #e9d5ff;
    padding: 0.4rem 0.5rem;
    border-radius: 6px;
    font-size: 0.75rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.15s ease;
    text-align: center;
  }

  .preset-pill:hover {
    background: rgba(168, 85, 247, 0.25);
    color: #ffffff;
  }

  .toggles-row {
    gap: 0.6rem;
  }

  .checkbox-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem !important;
    color: #cbd5e1 !important;
    cursor: pointer;
  }

  /* Winners History */
  .mini-clear-btn {
    background: none;
    border: none;
    color: #64748b;
    cursor: pointer;
    font-size: 0.7rem;
  }

  .mini-clear-btn:hover {
    color: #e2e8f0;
  }

  .winners-history-list {
    background: #090d16;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    padding: 0.5rem;
    max-height: 110px;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }

  .history-item {
    font-size: 0.78rem;
    color: #cbd5e1;
    display: flex;
    justify-content: space-between;
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.03);
  }

  .history-item .h-time {
    color: #64748b;
    font-size: 0.68rem;
  }

  .empty-history-text {
    font-size: 0.72rem;
    color: #475569;
    font-style: italic;
    text-align: center;
    padding: 0.4rem 0;
  }

  /* Celebration Modal */
  .winner-celebration-overlay {
    position: absolute;
    inset: 0;
    background: rgba(9, 13, 22, 0.85);
    backdrop-filter: blur(8px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 100;
  }

  .celebration-card {
    background: #0f172a;
    border: 2px solid rgba(234, 179, 8, 0.6);
    border-radius: 20px;
    padding: 2.5rem 2rem;
    text-align: center;
    max-width: 400px;
    width: 90%;
    box-shadow: 0 0 50px rgba(234, 179, 8, 0.4);
    animation: popModal 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes popModal {
    0% { transform: scale(0.6); opacity: 0; }
    100% { transform: scale(1); opacity: 1; }
  }

  .trophy-bounce {
    font-size: 3.8rem;
    margin-bottom: 0.5rem;
    animation: bounce 1s infinite alternate;
  }

  @keyframes bounce {
    0% { transform: translateY(0); }
    100% { transform: translateY(-8px); }
  }

  .congrats-tag {
    font-size: 0.85rem;
    font-weight: 800;
    color: #facc15;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
  }

  .winner-item-name {
    font-size: 2.2rem;
    font-weight: 900;
    color: #ffffff;
    letter-spacing: -0.02em;
    margin-bottom: 1.5rem;
    word-break: break-word;
    text-shadow: 0 0 20px rgba(250, 204, 21, 0.6);
  }

  .winner-confirm-btn {
    width: 100%;
    background: linear-gradient(135deg, #facc15 0%, #f97316 100%);
    color: #020617;
    font-size: 1.05rem;
    font-weight: 900;
    padding: 0.75rem;
    border-radius: 12px;
    border: none;
    cursor: pointer;
  }

  /* Confetti Layer */
  .confetti-layer {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 90;
  }

  .hidden {
    display: none !important;
  }

  @media (max-width: 1100px) {
    .wheel-stage-layout {
      flex-direction: column-reverse;
      gap: 1.25rem;
    }
    .roulette-prob-panel {
      width: 100% !important;
      max-width: 500px;
      height: 240px;
    }
  }

  @media (max-width: 900px) {
    
  /* Mode Switcher Tabs */
  .roulette-mode-tabs {
    grid-column: 1 / -1;
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    background: #0f172a;
    padding: 0.35rem;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
    margin-bottom: 0.5rem;
  }

  .mode-tab {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.85rem;
    font-weight: 800;
    color: #94a3b8;
    text-decoration: none;
    transition: all 0.15s ease;
  }

  .mode-tab:hover {
    color: #f8fafc;
    background: rgba(255, 255, 255, 0.05);
  }

  .mode-tab.active {
    background: #38bdf8;
    color: #040813;
    box-shadow: 0 0 15px rgba(56, 189, 248, 0.4);
  }

  .tab-badge {
    background: #ef4444;
    color: #ffffff;
    font-size: 0.65rem;
    font-weight: 900;
    padding: 0.1rem 0.35rem;
    border-radius: 4px;
    margin-left: 0.2rem;
  }

  .roulette-app-root {
      grid-template-columns: 1fr;
    }
    .wheel-canvas-wrapper {
      width: 340px;
      height: 340px;
    }
    #roulette-canvas {
      width: 330px;
      height: 330px;
    }
    .roulette-prob-panel {
      max-width: 340px;
    }
  }
</style>

<script>
  // Presets Data (Korean & English)
  const isEn = typeof window !== 'undefined' && new URLSearchParams(window.location.search).get('lang') === 'en';

  const PRESETS: Record<string, { title: string; items: string[] }> = {
    food: {
      title: '🍗 야식 / 배달 메뉴 룰렛',
      items: ['치킨', '피자', '햄버거', '족발/보쌈', '엽기떡볶이', '마라탕', '삼겹살 구이', '라면/분식', '초밥/회', '샐러드(양심)']
    },
    penalty: {
      title: '⚡ 스트리머 벌칙 룰렛',
      items: ['애교 3종 세트', '팔굽혀펴기 20회', '노방종 +1시간 추가', '노래 한 곡 완곡', '물 500ml 원샷', '영도 10분 금지', '시청자 소원권 1개', '벌칙 패스권 (럭키!)']
    },
    event: {
      title: '🎁 시청자 치킨/기프티콘 추첨',
      items: ['BBQ 황금올리브 치킨', '배민 1만원권', '스타벅스 아메리카노', '싸이버거 세트', '꽝 (다음 기회에)', '꽝 (아쉬워요)', '스타벅스 디저트 세트', '꽝 (재도전!)']
    },
    game: {
      title: '🎮 다음 할 게임 뽑기',
      items: ['리그 오브 레전드', '발로란트', '배틀그라운드', '스팀 신작 탐방', '공포 게임 (심야)', '종합 똥게임 챌린지', '시청자 참여 멀티', 'just chatting']
    }
  };

  const PRESETS_EN: Record<string, { title: string; items: string[] }> = {
    food: {
      title: '🍕 Food & Dinner Wheel',
      items: ['Pizza*2', 'Burger', 'Fried Chicken', 'Tacos', 'Sushi', 'Steak', 'Pasta', 'Ramen', 'Salad']
    },
    penalty: {
      title: '⚡ Streamer Penalty Wheel',
      items: ['Sing a song', '20 Push-ups', 'Stream +30m', 'Tell a secret', 'Drink water 500ml', 'Viewer wish', 'Safe pass (Lucky!)', 'Dance 15s']
    },
    event: {
      title: '🎁 Viewer Giveaway Wheel',
      items: ['Gift Card $20', 'Steam Game Key', 'Discord Nitro', 'Coffee Coupon', 'Try Again', 'Missed!', 'VIP Badge', 'Better Luck!']
    },
    game: {
      title: '🎮 Next Game to Play',
      items: ['Valorant', 'Minecraft', 'League of Legends', 'Apex Legends', 'Overwatch 2', 'Horror Game', 'Steam Indie', 'Just Chatting']
    }
  };

  // Color Palette for Slices
  const SLICE_COLORS = [
    '#ec4899', '#8b5cf6', '#3b82f6', '#06b6d4',
    '#10b981', '#84cc16', '#eab308', '#f97316',
    '#ef4444', '#a855f7', '#14b8a6', '#6366f1'
  ];

  // Web Audio Procedural Sound Synthesizer
  class SoundFX {
    private ctx: AudioContext | null = null;
    public enabled: boolean = true;

    constructor() {
      window.addEventListener('sound-toggle', (e: any) => {
        this.enabled = !e.detail?.muted;
      });
      window.addEventListener('storage', (e) => {
        if (e.key === 'aether_sound_muted') {
          this.enabled = localStorage.getItem('aether_sound_muted') !== 'true';
        }
      });
      this.enabled = localStorage.getItem('aether_sound_muted') !== 'true';
    }

    private isMuted(): boolean {
      try {
        return localStorage.getItem('aether_sound_muted') === 'true';
      } catch {
        return false;
      }
    }

    private init() {
      if (this.isMuted()) return;
      if (!this.ctx) {
        const AudioCtx = window.AudioContext || (window as any).webkitAudioContext;
        if (AudioCtx) this.ctx = new AudioCtx();
      }
      if (this.ctx && this.ctx.state === 'suspended') {
        this.ctx.resume();
      }
    }

    playTick() {
      if (!this.enabled) return;
      this.init();
      if (!this.ctx) return;
      const osc = this.ctx.createOscillator();
      const gain = this.ctx.createGain();
      osc.type = 'triangle';
      osc.frequency.setValueAtTime(600, this.ctx.currentTime);
      osc.frequency.exponentialRampToValueAtTime(120, this.ctx.currentTime + 0.04);
      gain.gain.setValueAtTime(0.2, this.ctx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.01, this.ctx.currentTime + 0.04);
      osc.connect(gain);
      gain.connect(this.ctx.destination);
      osc.start();
      osc.stop(this.ctx.currentTime + 0.04);
    }

    playWinFanfare() {
      if (!this.enabled) return;
      this.init();
      if (!this.ctx) return;
      const notes = [523.25, 659.25, 783.99, 1046.50]; // C5, E5, G5, C6
      notes.forEach((freq, idx) => {
        const osc = this.ctx!.createOscillator();
        const gain = this.ctx!.createGain();
        osc.type = 'sine';
        osc.frequency.setValueAtTime(freq, this.ctx!.currentTime + idx * 0.1);
        gain.gain.setValueAtTime(0.3, this.ctx!.currentTime + idx * 0.1);
        gain.gain.exponentialRampToValueAtTime(0.01, this.ctx!.currentTime + idx * 0.1 + 0.35);
        osc.connect(gain);
        gain.connect(this.ctx!.destination);
        osc.start(this.ctx!.currentTime + idx * 0.1);
        osc.stop(this.ctx!.currentTime + idx * 0.1 + 0.35);
      });
    }
  }

  const sfx = new SoundFX();

  // Particle Confetti Engine
  class ConfettiEngine {
    private canvas: HTMLCanvasElement;
    private ctx: CanvasRenderingContext2D | null;
    private particles: { x: number; y: number; vx: number; vy: number; color: string; size: number; rot: number; vrot: number }[] = [];
    private running: boolean = false;

    constructor(canvas: HTMLCanvasElement) {
      this.canvas = canvas;
      this.ctx = canvas.getContext('2d');
    }

    burst() {
      this.canvas.width = this.canvas.offsetWidth;
      this.canvas.height = this.canvas.offsetHeight;
      this.particles = [];
      const colors = ['#facc15', '#ec4899', '#38bdf8', '#a855f7', '#10b981', '#f97316'];
      for (let i = 0; i < 90; i++) {
        this.particles.push({
          x: this.canvas.width / 2,
          y: this.canvas.height / 2,
          vx: (Math.random() - 0.5) * 14,
          vy: (Math.random() - 0.5) * 14 - 3,
          color: colors[Math.floor(Math.random() * colors.length)],
          size: Math.random() * 8 + 4,
          rot: Math.random() * Math.PI,
          vrot: (Math.random() - 0.5) * 0.2
        });
      }
      this.running = true;
      this.loop();
    }

    private loop = () => {
      if (!this.ctx || !this.running) return;
      this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
      let alive = 0;
      for (const p of this.particles) {
        p.vy += 0.3; // gravity
        p.x += p.vx;
        p.y += p.vy;
        p.rot += p.vrot;

        if (p.y < this.canvas.height) {
          alive++;
          this.ctx.save();
          this.ctx.translate(p.x, p.y);
          this.ctx.rotate(p.rot);
          this.ctx.fillStyle = p.color;
          this.ctx.fillRect(-p.size / 2, -p.size / 2, p.size, p.size);
          this.ctx.restore();
        }
      }

      if (alive > 0) {
        requestAnimationFrame(this.loop);
      } else {
        this.running = false;
        this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
      }
    };
  }

  // Festival Particle Sparkles Engine (Carnival Lights & Star Trails)
  class FestivalSparklesEngine {
    private canvas: HTMLCanvasElement;
    private ctx: CanvasRenderingContext2D | null;
    private particles: { x: number; y: number; vx: number; vy: number; color: string; size: number; alpha: number; life: number; maxLife: number; shape: 'star' | 'circle' }[] = [];
    private running: boolean = false;

    constructor(canvas: HTMLCanvasElement) {
      this.canvas = canvas;
      this.ctx = canvas ? canvas.getContext('2d') : null;
      this.resize();
      window.addEventListener('resize', () => this.resize());
    }

    private resize() {
      if (!this.canvas) return;
      this.canvas.width = window.innerWidth;
      this.canvas.height = window.innerHeight;
    }

    spawnSparksAroundWheel(cx: number, cy: number, radius: number, color: string) {
      if (!this.ctx || !this.canvas) return;
      const count = 3;
      const festiveColors = [color, '#facc15', '#ffffff', '#38bdf8', '#f43f5e', '#a855f7'];
      for (let i = 0; i < count; i++) {
        const angle = Math.random() * Math.PI * 2;
        const r = radius + (Math.random() - 0.5) * 20;
        const x = cx + Math.cos(angle) * r;
        const y = cy + Math.sin(angle) * r;
        const c = festiveColors[Math.floor(Math.random() * festiveColors.length)];

        this.particles.push({
          x,
          y,
          vx: (Math.random() - 0.5) * 4 + Math.cos(angle) * 1.5,
          vy: (Math.random() - 0.5) * 4 + Math.sin(angle) * 1.5 - 1.2,
          color: c,
          size: Math.random() * 5 + 3,
          alpha: 1,
          life: 0,
          maxLife: 22 + Math.random() * 18,
          shape: Math.random() > 0.35 ? 'star' : 'circle'
        });
      }

      if (!this.running) {
        this.running = true;
        this.loop();
      }
    }

    burstVictoryStars(cx: number, cy: number) {
      if (!this.ctx || !this.canvas) return;
      const festiveColors = ['#facc15', '#f59e0b', '#fef08a', '#ffffff', '#f43f5e', '#38bdf8'];
      for (let i = 0; i < 40; i++) {
        const angle = Math.random() * Math.PI * 2;
        const speed = Math.random() * 8 + 3;
        this.particles.push({
          x: cx,
          y: cy,
          vx: Math.cos(angle) * speed,
          vy: Math.sin(angle) * speed - 2.5,
          color: festiveColors[Math.floor(Math.random() * festiveColors.length)],
          size: Math.random() * 7 + 4,
          alpha: 1,
          life: 0,
          maxLife: 35 + Math.random() * 25,
          shape: 'star'
        });
      }
      if (!this.running) {
        this.running = true;
        this.loop();
      }
    }

    private drawStar(cx: number, cy: number, spikes: number, outerRadius: number, innerRadius: number, color: string, alpha: number) {
      if (!this.ctx) return;
      let rot = (Math.PI / 2) * 3;
      let x = cx;
      let y = cy;
      const step = Math.PI / spikes;

      this.ctx.save();
      this.ctx.globalAlpha = alpha;
      this.ctx.fillStyle = color;
      this.ctx.beginPath();
      this.ctx.moveTo(cx, cy - outerRadius);
      for (let i = 0; i < spikes; i++) {
        x = cx + Math.cos(rot) * outerRadius;
        y = cy + Math.sin(rot) * outerRadius;
        this.ctx.lineTo(x, y);
        rot += step;

        x = cx + Math.cos(rot) * innerRadius;
        y = cy + Math.sin(rot) * innerRadius;
        this.ctx.lineTo(x, y);
        rot += step;
      }
      this.ctx.lineTo(cx, cy - outerRadius);
      this.ctx.closePath();
      this.ctx.fill();
      this.ctx.restore();
    }

    private loop = () => {
      if (!this.ctx || !this.running) return;
      this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);

      let alive = 0;
      for (let i = 0; i < this.particles.length; i++) {
        const p = this.particles[i];
        p.life++;
        p.x += p.vx;
        p.y += p.vy;
        p.alpha = Math.max(0, 1 - (p.life / p.maxLife));

        if (p.alpha > 0) {
          alive++;
          if (p.shape === 'star') {
            this.drawStar(p.x, p.y, 4, p.size, p.size * 0.45, p.color, p.alpha);
          } else {
            this.ctx.save();
            this.ctx.globalAlpha = p.alpha;
            this.ctx.fillStyle = p.color;
            this.ctx.beginPath();
            this.ctx.arc(p.x, p.y, p.size / 2, 0, Math.PI * 2);
            this.ctx.fill();
            this.ctx.restore();
          }
        }
      }

      this.particles = this.particles.filter(p => p.alpha > 0);

      if (alive > 0) {
        requestAnimationFrame(this.loop);
      } else {
        this.running = false;
        this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
      }
    };
  }

  // DOM Elements
  const canvas = document.getElementById('roulette-canvas') as HTMLCanvasElement;
  const ctx = canvas?.getContext('2d');
  const pointerElem = document.getElementById('wheel-pointer');
  const confettiCanvas = document.getElementById('confetti-canvas') as HTMLCanvasElement;
  const confetti = new ConfettiEngine(confettiCanvas);
  const carnivalCanvas = document.getElementById('carnival-canvas') as HTMLCanvasElement;
  const festivalSparkles = new FestivalSparklesEngine(carnivalCanvas);

  const probItemsList = document.getElementById('prob-items-list');
  const probTotalBadge = document.getElementById('prob-total-badge');
  const probTitleText = document.getElementById('prob-title-text');
  const pointerTicker = document.getElementById('pointer-ticker');
  const tickerDot = document.getElementById('ticker-dot');
  const tickerText = document.getElementById('ticker-text');

  const titleInput = document.getElementById('input-title') as HTMLInputElement;
  const itemsTextarea = document.getElementById('input-items') as HTMLTextAreaElement;
  const displayTitle = document.getElementById('display-title');
  const itemsCountBadge = document.getElementById('items-count-badge');
  const chkEliminate = document.getElementById('chk-eliminate') as HTMLInputElement;
  const chkSound = document.getElementById('chk-sound') as HTMLInputElement;

  const mainSpinBtn = document.getElementById('main-spin-btn');
  const centerSpinBtn = document.getElementById('center-spin-btn');
  const shuffleBtn = document.getElementById('shuffle-btn');
  const streamModeBtn = document.getElementById('stream-mode-btn');

  const winnerModal = document.getElementById('winner-modal');
  const winnerNameDisplay = document.getElementById('winner-name-display');
  const modalConfirmBtn = document.getElementById('modal-confirm-btn');
  const winnersHistory = document.getElementById('winners-history');
  const clearHistoryBtn = document.getElementById('clear-history-btn');

  let items: string[] = [];
  let currentAngle = 0; // In radians
  let isSpinning = false;
  let lastTickedSlice = -1;

  function parseItems(): string[] {
    if (!itemsTextarea) return [];
    const rawLines = itemsTextarea.value
      .split(/[\r\n,]+/)
      .map(s => s.trim())
      .filter(s => s.length > 0);

    const result: string[] = [];
    for (const line of rawLines) {
      // Check for multiplier patterns: "피자*2", "치킨 * 3", "꽝x5", "콜라×4"
      const match = line.match(/^(.+?)(?:\s*[\*xX×]\s*(\d+))$/);
      if (match) {
        const name = match[1].trim();
        const count = Math.min(Math.max(parseInt(match[2], 10) || 1, 1), 100);
        for (let i = 0; i < count; i++) {
          result.push(name);
        }
      } else {
        result.push(line);
      }
    }
    return result;
  }

  function removeOneInstanceFromTextarea(winner: string) {
    if (!itemsTextarea) return;
    const lines = itemsTextarea.value.split('\n');
    let removed = false;
    const newLines: string[] = [];

    for (const line of lines) {
      const trimmed = line.trim();
      if (!removed) {
        const match = trimmed.match(/^(.+?)(?:\s*[\*xX×]\s*(\d+))$/);
        if (match && match[1].trim() === winner) {
          const count = parseInt(match[2], 10) || 1;
          if (count > 2) {
            newLines.push(`${winner}*${count - 1}`);
          } else if (count === 2) {
            newLines.push(winner);
          }
          removed = true;
          continue;
        } else if (trimmed === winner) {
          removed = true;
          continue;
        }
      }
      if (trimmed.length > 0) newLines.push(line);
    }
    itemsTextarea.value = newLines.join('\n');
  }

  // Probability Calculation & Dynamic List Rendering
  interface ItemProbGroup {
    name: string;
    count: number;
    probPercent: string;
    color: string;
  }

  function calculateProbabilities(): ItemProbGroup[] {
    if (items.length === 0) return [];
    const counts: Record<string, { count: number; firstIndex: number }> = {};
    items.forEach((item, idx) => {
      if (!counts[item]) {
        counts[item] = { count: 1, firstIndex: idx };
      } else {
        counts[item].count++;
      }
    });

    const total = items.length;
    const groups: ItemProbGroup[] = Object.keys(counts).map(name => {
      const c = counts[name].count;
      const pct = ((c / total) * 100).toFixed(1);
      const color = SLICE_COLORS[counts[name].firstIndex % SLICE_COLORS.length];
      return {
        name,
        count: c,
        probPercent: pct,
        color
      };
    });

    // Sort by count descending, then alphabetical
    groups.sort((a, b) => b.count - a.count);
    return groups;
  }

  function renderProbabilityList() {
    if (!probItemsList) return;
    const groups = calculateProbabilities();

    if (probTotalBadge) {
      probTotalBadge.textContent = isEn ? `Total ${items.length} Slices` : `총 ${items.length}개 칸`;
    }

    if (groups.length === 0) {
      probItemsList.innerHTML = `<div class="prob-empty-text">${isEn ? 'No items entered' : '등록된 항목이 없습니다'}</div>`;
      return;
    }

    probItemsList.innerHTML = groups.map(g => `
      <div class="prob-item-row" data-item-name="${escapeHtml(g.name)}" style="--item-color: ${g.color};">
        <span class="prob-color-dot" style="background: ${g.color};"></span>
        <span class="prob-item-name" title="${escapeHtml(g.name)}">${escapeHtml(g.name)}</span>
        ${g.count > 1 ? `<span class="prob-item-count">${g.count}${isEn ? 'x' : '칸'}</span>` : ''}
        <div class="prob-inline-bar-track">
          <div class="prob-inline-bar-fill" style="width: ${g.probPercent}%; background: ${g.color};"></div>
        </div>
        <span class="prob-item-percent">${g.probPercent}%</span>
      </div>
    `).join('');
  }

  function highlightProbItem(itemName: string) {
    if (!probItemsList) return;
    const rows = probItemsList.querySelectorAll('.prob-item-row');
    rows.forEach(r => {
      if (r.getAttribute('data-item-name') === itemName) {
        r.classList.add('is-active-slice');
      } else {
        r.classList.remove('is-active-slice');
      }
    });
  }

  function escapeHtml(str: string): string {
    return str
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#039;');
  }

  function syncItems() {
    items = parseItems();
    if (itemsCountBadge) itemsCountBadge.textContent = isEn ? `${items.length} Items` : `${items.length}개 항목`;
    renderProbabilityList();
    drawWheel();
    if (!isSpinning) {
      if (pointerTicker) pointerTicker.classList.remove('is-winner');
      if (tickerText) {
        tickerText.textContent = items.length > 0 ? (isEn ? 'Ready (Space)' : '준비 완료 (Space)') : (isEn ? 'Enter items' : '항목을 입력하세요');
      }
      if (tickerDot) {
        tickerDot.style.background = '#38bdf8';
        tickerDot.style.boxShadow = '0 0 8px #38bdf8';
      }
      if (pointerTicker) {
        pointerTicker.style.setProperty('--ticker-color', '#38bdf8');
      }
    }
  }

  function drawWheel() {
    if (!ctx) return;
    const width = canvas.width;
    const height = canvas.height;
    const centerX = width / 2;
    const centerY = height / 2;
    const outerCasingRadius = width / 2 - 14; // 286
    const bulbRingRadius = outerCasingRadius - 16; // 270
    const sliceRadius = bulbRingRadius - 18; // 252

    ctx.clearRect(0, 0, width, height);

    if (items.length === 0) {
      // Empty wheel state
      ctx.beginPath();
      ctx.arc(centerX, centerY, sliceRadius, 0, Math.PI * 2);
      ctx.fillStyle = '#1e293b';
      ctx.fill();
      ctx.strokeStyle = '#334155';
      ctx.lineWidth = 6;
      ctx.stroke();

      ctx.fillStyle = '#64748b';
      ctx.font = 'bold 20px sans-serif';
      ctx.textAlign = 'center';
      ctx.fillText(isEn ? 'Please enter items' : '옵션을 입력해주세요', centerX, centerY);
      return;
    }

    const sliceAngle = (Math.PI * 2) / items.length;

    // 1. Draw Outer Carnival Metallic Rim & Track
    ctx.beginPath();
    ctx.arc(centerX, centerY, outerCasingRadius, 0, Math.PI * 2);
    ctx.fillStyle = '#090d16';
    ctx.fill();
    ctx.strokeStyle = isSpinning ? '#eab308' : '#334155';
    ctx.lineWidth = 4;
    ctx.stroke();

    ctx.beginPath();
    ctx.arc(centerX, centerY, bulbRingRadius + 8, 0, Math.PI * 2);
    ctx.strokeStyle = '#1e293b';
    ctx.lineWidth = 14;
    ctx.stroke();

    // 2. Carnival Marquee Lights (32 bulbs around perimeter)
    const numBulbs = 32;
    const bulbColors = ['#facc15', '#38bdf8', '#f43f5e', '#34d399'];
    for (let b = 0; b < numBulbs; b++) {
      const bAngle = (b / numBulbs) * Math.PI * 2;
      const bx = centerX + Math.cos(bAngle) * bulbRingRadius;
      const by = centerY + Math.sin(bAngle) * bulbRingRadius;

      let bulbColor = '#fef08a';
      let bulbGlow = 'rgba(250, 204, 21, 0.6)';

      if (isSpinning) {
        const chaseIndex = (b + Math.floor(currentAngle * 7)) % 4;
        bulbColor = bulbColors[chaseIndex];
        bulbGlow = bulbColor;
      } else {
        bulbColor = (b % 2 === 0) ? '#facc15' : '#fef08a';
      }

      // Bulb Socket
      ctx.beginPath();
      ctx.arc(bx, by, 5.5, 0, Math.PI * 2);
      ctx.fillStyle = '#020617';
      ctx.fill();

      // Glowing Bulb
      ctx.save();
      ctx.beginPath();
      ctx.arc(bx, by, 3.8, 0, Math.PI * 2);
      ctx.fillStyle = bulbColor;
      ctx.shadowColor = bulbGlow;
      ctx.shadowBlur = isSpinning ? 12 : 5;
      ctx.fill();
      ctx.restore();
    }

    // 3. Draw Slices
    for (let i = 0; i < items.length; i++) {
      const startAngle = currentAngle + i * sliceAngle;
      const endAngle = startAngle + sliceAngle;
      const color = SLICE_COLORS[i % SLICE_COLORS.length];

      // Draw Slice Pie
      ctx.beginPath();
      ctx.moveTo(centerX, centerY);
      ctx.arc(centerX, centerY, sliceRadius, startAngle, endAngle);
      ctx.closePath();
      ctx.fillStyle = color;
      ctx.fill();
      ctx.strokeStyle = '#0f172a';
      ctx.lineWidth = 2.5;
      ctx.stroke();

      // Draw Slice Text
      ctx.save();
      ctx.translate(centerX, centerY);
      ctx.rotate(startAngle + sliceAngle / 2);
      ctx.textAlign = 'right';
      ctx.fillStyle = '#ffffff';
      let text = items[i];
      let fontSize = items.length > 14 ? 12 : (items.length > 8 ? 14 : 16);
      if (text.length > 12) {
        fontSize = Math.max(10, fontSize - 2);
      }
      if (text.length > 18) text = text.substring(0, 17) + '…';
      ctx.font = `bold ${fontSize}px sans-serif`;
      ctx.fillText(text, sliceRadius - 18, 5);
      ctx.restore();
    }

    // 4. Center Bezel behind center button
    ctx.beginPath();
    ctx.arc(centerX, centerY, 48, 0, Math.PI * 2);
    ctx.fillStyle = '#0f172a';
    ctx.fill();
    ctx.strokeStyle = 'rgba(250, 204, 21, 0.6)';
    ctx.lineWidth = 3;
    ctx.stroke();
  }

  function spinWheel() {
    if (isSpinning || items.length < 2) return;
    isSpinning = true;

    if (pointerTicker) {
      pointerTicker.classList.remove('is-winner');
    }

    // Random spin parameters
    const minRounds = 5;
    const maxRounds = 9;
    const extraRounds = minRounds + Math.random() * (maxRounds - minRounds);
    const targetDeltaAngle = extraRounds * Math.PI * 2 + Math.random() * Math.PI * 2;
    const duration = 4800 + Math.random() * 800; // ms
    const startTime = performance.now();
    const startAngle = currentAngle;

    function anim(now: number) {
      const elapsed = now - startTime;
      const progress = Math.min(1, elapsed / duration);

      // Ease out cubic deceleration
      const ease = 1 - Math.pow(1 - progress, 3.2);
      currentAngle = startAngle + targetDeltaAngle * ease;

      // Calculate current slice under top pointer (Pointer is at -PI/2 or 270 deg)
      const normalized = ((currentAngle % (Math.PI * 2)) + Math.PI * 2) % (Math.PI * 2);
      const pointerAngle = (Math.PI * 1.5 - normalized + Math.PI * 2) % (Math.PI * 2);
      const sliceAngle = (Math.PI * 2) / items.length;
      const currentSlice = Math.floor(pointerAngle / sliceAngle);
      const currentItem = items[currentSlice] || '';
      const currentColor = SLICE_COLORS[currentSlice % SLICE_COLORS.length];

      // Real-time live ticker update on top of pointer needle!
      if (tickerText && currentItem) {
        tickerText.textContent = currentItem;
      }
      if (tickerDot) {
        tickerDot.style.background = currentColor;
        tickerDot.style.boxShadow = `0 0 8px ${currentColor}`;
      }
      if (pointerTicker) {
        pointerTicker.style.setProperty('--ticker-color', currentColor);
      }

      // Play tick sound when slice boundary crossed
      if (currentSlice !== lastTickedSlice) {
        lastTickedSlice = currentSlice;
        sfx.playTick();
        if (pointerElem) {
          pointerElem.classList.remove('pointer-tick');
          void pointerElem.offsetWidth;
          pointerElem.classList.add('pointer-tick');
        }
        if (pointerTicker) {
          pointerTicker.classList.remove('ticker-pulse');
          void pointerTicker.offsetWidth;
          pointerTicker.classList.add('ticker-pulse');
        }

        // Live highlight corresponding item in probability panel!
        highlightProbItem(currentItem);

        // Festival Sparkles: emit sparks around wheel edge
        const rect = canvas.getBoundingClientRect();
        festivalSparkles.spawnSparksAroundWheel(
          rect.left + rect.width / 2,
          rect.top + rect.height / 2,
          rect.width / 2,
          currentColor
        );
      }

      drawWheel();

      if (progress < 1) {
        requestAnimationFrame(anim);
      } else {
        isSpinning = false;
        const winnerIndex = currentSlice;
        const winner = items[winnerIndex];
        finishSpin(winner, winnerIndex);
      }
    }

    requestAnimationFrame(anim);
  }

  function finishSpin(winner: string, index: number) {
    sfx.playWinFanfare();
    confetti.burst();

    // Victory stars from wheel center!
    const rect = canvas.getBoundingClientRect();
    festivalSparkles.burstVictoryStars(rect.left + rect.width / 2, rect.top + rect.height / 2);

    // Update live ticker with winner celebration!
    if (tickerText) tickerText.textContent = `👑 ${winner}`;
    if (pointerTicker) pointerTicker.classList.add('is-winner');

    // Highlight winning item in probability list
    highlightProbItem(winner);

    // Show winner banner
    if (winnerNameDisplay) winnerNameDisplay.textContent = winner;
    if (winnerModal) winnerModal.classList.remove('hidden');

    // Add to history
    addHistoryItem(winner);

    // If eliminate winner checked
    if (chkEliminate && chkEliminate.checked) {
      removeOneInstanceFromTextarea(winner);
      syncItems();
    }
  }

  function addHistoryItem(winner: string) {
    if (!winnersHistory) return;
    const emptyHint = winnersHistory.querySelector('.empty-history-text');
    if (emptyHint) emptyHint.remove();

    const item = document.createElement('div');
    item.className = 'history-item';
    const now = new Date();
    const timeStr = `${now.getHours()}:${String(now.getMinutes()).padStart(2, '0')}:${String(now.getSeconds()).padStart(2, '0')}`;
    item.innerHTML = `<span>🏆 <strong>${winner}</strong></span><span class="h-time">${timeStr}</span>`;
    winnersHistory.prepend(item);
  }

  // Preset Handlers
  const activePresets = isEn ? PRESETS_EN : PRESETS;

  document.querySelectorAll('.preset-pill').forEach(btn => {
    btn.addEventListener('click', () => {
      const key = (btn as HTMLElement).dataset.preset || '';
      const preset = activePresets[key];
      if (preset) {
        if (titleInput) titleInput.value = preset.title;
        if (displayTitle) displayTitle.textContent = preset.title;
        if (itemsTextarea) itemsTextarea.value = preset.items.join('\n');
        syncItems();
      }
    });
  });

  // Event Listeners
  titleInput?.addEventListener('input', () => {
    if (displayTitle) displayTitle.textContent = titleInput.value || (isEn ? "Today's Spin Wheel" : '오늘의 룰렛');
  });

  itemsTextarea?.addEventListener('input', syncItems);

  shuffleBtn?.addEventListener('click', () => {
    items.sort(() => Math.random() - 0.5);
    if (itemsTextarea) itemsTextarea.value = items.join('\n');
    syncItems();
  });

  mainSpinBtn?.addEventListener('click', spinWheel);
  centerSpinBtn?.addEventListener('click', spinWheel);

  modalConfirmBtn?.addEventListener('click', () => {
    if (winnerModal) winnerModal.classList.add('hidden');
  });

  clearHistoryBtn?.addEventListener('click', () => {
    if (winnersHistory) winnersHistory.innerHTML = `<span class="empty-history-text">${isEn ? 'No winners yet. Spin the wheel!' : '아직 당첨 기록이 없습니다. 룰렛을 돌려보세요!'}</span>`;
  });

  chkSound?.addEventListener('change', () => {
    sfx.enabled = chkSound.checked;
  });

  // Stream Overlay Mode Toggle
  const sidebar = document.getElementById('streamer-sidebar');
  streamModeBtn?.addEventListener('click', () => {
    if (sidebar) {
      sidebar.classList.toggle('hidden');
      streamModeBtn.classList.toggle('btn-secondary');
    }
  });

  // Spacebar to Spin
  window.addEventListener('keydown', (e) => {
    if (e.code === 'Space' && document.activeElement !== itemsTextarea && document.activeElement !== titleInput) {
      e.preventDefault();
      if (!winnerModal?.classList.contains('hidden')) {
        winnerModal?.classList.add('hidden');
      } else {
        spinWheel();
      }
    }
  });

  // English UI Localization
  if (isEn) {
    if (centerSpinBtn) {
      const hubLabel = centerSpinBtn.querySelector('.hub-label');
      if (hubLabel) hubLabel.textContent = 'SPIN';
      centerSpinBtn.setAttribute('title', 'Click or press Space to spin wheel');
    }
    if (mainSpinBtn) {
      const span = mainSpinBtn.querySelector('span');
      if (span) span.textContent = '🎯 Spin Wheel (Space)';
    }
    if (shuffleBtn) {
      const span = shuffleBtn.querySelector('span');
      if (span) span.textContent = '🔀 Shuffle Items';
    }
    if (streamModeBtn) {
      const span = streamModeBtn.querySelector('span');
      if (span) span.textContent = '📺 Focus Mode';
      streamModeBtn.setAttribute('title', 'Toggle minimal focus mode');
    }
    const deskHeader = document.querySelector('.sidebar-header h3');
    if (deskHeader) deskHeader.textContent = 'Wheel Settings Desk';

    const titleLabel = document.querySelector('label[for="input-title"]');
    if (titleLabel) titleLabel.textContent = 'Wheel Title';

    if (titleInput && (!titleInput.value || titleInput.value === '오늘의 방송 룰렛')) {
      titleInput.value = "Today's Spin Wheel";
      titleInput.placeholder = 'Enter wheel title';
    }
    if (displayTitle && (!displayTitle.textContent || displayTitle.textContent.includes('오늘의 방송'))) {
      displayTitle.textContent = "Today's Spin Wheel";
    }

    const presetLabel = document.querySelector('.presets-grid')?.previousElementSibling;
    if (presetLabel) presetLabel.textContent = '⚡ Stream Presets';

    const pFood = document.querySelector('.preset-pill[data-preset="food"]');
    if (pFood) pFood.textContent = '🍕 Food Menu';
    const pPenalty = document.querySelector('.preset-pill[data-preset="penalty"]');
    if (pPenalty) pPenalty.textContent = '⚡ Penalties';
    const pEvent = document.querySelector('.preset-pill[data-preset="event"]');
    if (pEvent) pEvent.textContent = '🎁 Giveaways';
    const pGame = document.querySelector('.preset-pill[data-preset="game"]');
    if (pGame) pGame.textContent = '🎮 Game Picker';

    const itemsLabel = document.querySelector('label[for="input-items"]');
    if (itemsLabel) itemsLabel.textContent = 'Item List';

    const hintText = document.querySelector('.input-hint-text');
    if (hintText) hintText.innerHTML = '💡 Tip: Enter <code>Pizza*2</code> or <code>Burger*3</code> to give items multiple slices!';

    if (itemsTextarea) {
      itemsTextarea.placeholder = 'Enter items separated by newlines (e.g. Pizza*2, Burger*3, Prize*5)';
      if (itemsTextarea.value.includes('치킨')) {
        itemsTextarea.value = PRESETS_EN.food.items.join('\n');
      }
    }

    const chkEliminateLabel = chkEliminate?.parentElement?.querySelector('span');
    if (chkEliminateLabel) chkEliminateLabel.textContent = 'Remove won item automatically';

    const chkSoundLabel = chkSound?.parentElement?.querySelector('span');
    if (chkSoundLabel) chkSoundLabel.textContent = 'Sound Effects (Tick & Fanfare)';

    const historyLabel = document.querySelector('.history-group .label-with-meta label');
    if (historyLabel) historyLabel.textContent = '🏆 Winner History';

    if (clearHistoryBtn) clearHistoryBtn.textContent = 'Clear';

    const emptyHint = document.querySelector('.empty-history-text');
    if (emptyHint) emptyHint.textContent = 'No winners yet. Spin the wheel!';

    const congratsTag = document.querySelector('.congrats-tag');
    if (congratsTag) congratsTag.textContent = '🎉 Congratulations!';

    if (modalConfirmBtn) modalConfirmBtn.textContent = 'Close (OK)';
  }

  // Carnival Golden Sparkles Background Animation
  function initCarnivalSparkles() {
    const bgCanvas = document.getElementById('carnival-canvas') as HTMLCanvasElement;
    if (!bgCanvas) return;
    const bgCtx = bgCanvas.getContext('2d');
    if (!bgCtx) return;

    function resize() {
      if (!bgCanvas) return;
      bgCanvas.width = window.innerWidth;
      bgCanvas.height = window.innerHeight;
    }
    window.addEventListener('resize', resize);
    resize();

    interface Sparkle {
      x: number;
      y: number;
      size: number;
      speedY: number;
      speedX: number;
      color: string;
      alpha: number;
      alphaSpeed: number;
      pulsePhase: number;
      isStar: boolean;
    }

    const sparkleColors = ['#facc15', '#fbbf24', '#38bdf8', '#c084fc', '#f472b6', '#ffffff'];
    const sparkles: Sparkle[] = [];
    const count = 38;

    for (let i = 0; i < count; i++) {
      sparkles.push({
        x: Math.random() * bgCanvas.width,
        y: Math.random() * bgCanvas.height,
        size: Math.random() * 3.5 + 1.2,
        speedY: -(Math.random() * 0.6 + 0.2), // gentle upward float
        speedX: Math.random() * 0.4 - 0.2,
        color: sparkleColors[Math.floor(Math.random() * sparkleColors.length)],
        alpha: Math.random() * 0.6 + 0.2,
        alphaSpeed: Math.random() * 0.02 + 0.01,
        pulsePhase: Math.random() * Math.PI * 2,
        isStar: Math.random() > 0.45,
      });
    }

    function renderSparkles() {
      if (!bgCtx || !bgCanvas) return;
      bgCtx.clearRect(0, 0, bgCanvas.width, bgCanvas.height);

      sparkles.forEach(s => {
        s.y += s.speedY;
        s.x += s.speedX;
        s.pulsePhase += s.alphaSpeed;

        if (s.y < -20) {
          s.y = bgCanvas.height + 20;
          s.x = Math.random() * bgCanvas.width;
        }
        if (s.x < -20) s.x = bgCanvas.width + 20;
        if (s.x > bgCanvas.width + 20) s.x = -20;

        const currentAlpha = Math.max(0.1, Math.min(0.85, s.alpha + Math.sin(s.pulsePhase) * 0.3));

        bgCtx.save();
        bgCtx.translate(s.x, s.y);
        bgCtx.globalAlpha = currentAlpha;
        bgCtx.fillStyle = s.color;

        if (s.isStar) {
          // 4-pointed sparkle diamond
          const r = s.size * 1.5;
          bgCtx.beginPath();
          bgCtx.moveTo(0, -r);
          bgCtx.lineTo(r * 0.3, -r * 0.3);
          bgCtx.lineTo(r, 0);
          bgCtx.lineTo(r * 0.3, r * 0.3);
          bgCtx.lineTo(0, r);
          bgCtx.lineTo(-r * 0.3, r * 0.3);
          bgCtx.lineTo(-r, 0);
          bgCtx.lineTo(-r * 0.3, -r * 0.3);
          bgCtx.closePath();
          bgCtx.fill();
        } else {
          // Glowing orb
          bgCtx.beginPath();
          bgCtx.arc(0, 0, s.size, 0, Math.PI * 2);
          bgCtx.fill();
        }
        bgCtx.restore();
      });

      requestAnimationFrame(renderSparkles);
    }
    requestAnimationFrame(renderSparkles);
  }

  // Initial setup
  initCarnivalSparkles();
  syncItems();
</script>
